# ⚠️ 必填：SEC 与国会披露站点要求请求带可识别的 User-Agent（含联系方式）。
# 未配置时程序会直接报错并给出提示，而不是偷偷用一个占位值 ——
# 那会让你在毫不知情的情况下被上游限流。
#
# 这只是给上游站点识别**你自己**的身份，不会发送到任何第三方。
VF_CONTACT="Your Name your@email.com"

# 可选：本地数据存放目录（默认 ~/.vibe-flow）
# VF_DATA_DIR="/path/to/data"

# 可选：启用 FINRA 数据源（做空的场外空头量 + 暗池/场外分栏）。
# ⚠️ **默认关闭是刻意的。** FINRA Terms of Use 限「仅供非商业的个人或专业用途」，
#    且限制 (d) 明文禁止「用本站数据建立数据库」—— 而本项目正是下载→落 SQLite。
#    条款原文与模糊之处在界面上原样摆着，判断权在你，我们不替你解释条款。
#    不开启时：做空分栏照常用 SEC 的交割失败数据；暗池分栏只显示条款与开启方法。
# VF_ENABLE_FINRA=1
