# Python 3.9+ (zoneinfo and str.removesuffix are the hard floor; nothing 3.10-only is used).
# Enforced at startup by backend/pyversion.py, which says so in plain words.
# ⚠️ FZ_CONTACT must be set before starting — see .env.example at the repo root.
#    SEC and the congressional sites want a UA carrying contact details; unset, it errors out.
# FloorZero backend dependencies — deliberately minimal.
fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
pypdf>=4.0            # House PTRs are PDFs; pure Python, no poppler system binary needed

# ── optional ──
# curl_cffi>=0.6      # Senate only: eFD is behind Akamai TLS fingerprinting, and
#                     # requests gets 403 no matter how the headers are dressed up.
#                     # Without it the Senate lane says "unavailable + how to install"; the House still works.
