# Development dependencies. Running the app needs only the four in requirements.txt.
#
# Run the tests:
#     pip install -r requirements-dev.txt
#     python -m pytest
#
# Tests touch no network and never write to ~/.floorzero — every case that stores goes to a throwaway dir.
pytest>=8.0
