[pytest]
testpaths = tests
# ⚠️ No network in tests: what is under test is the arithmetic and the semantics,
#    not whether an upstream is reachable today. That belongs to runtime, not unit tests.
addopts = -q --strict-markers
